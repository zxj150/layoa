<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context" xmlns:mvc="http://www.springframework.org/schema/mvc" xsi:schemaLocation="http://www.springframework.org/schema/beans 
	 http://www.springframework.org/schema/beans/spring-beans.xsd
	 http://www.springframework.org/schema/context
     http://www.springframework.org/schema/context/spring-context.xsd  
     http://www.springframework.org/schema/mvc    
     http://www.springframework.org/schema/mvc/spring-mvc.xsd ">
	<context:component-scan base-package="com.situ.user">
		<!-- exclude-filter 配置不用处理的 annotation -->
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service" />
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository" />
	</context:component-scan>
	<!-- <mvc:annotation-driven /> 会自动注册RequestMappingHandlerMapping与RequestMappingHandlerAdapter 两个bean -->
	<mvc:annotation-driven />
	<!-- Spring MVC 不拦截次配置下的文件 -->
	<!-- 将SpringMVC上下文中定义一个请求进行筛查,如果发现是没有经过映射的请求,就将请求交由WEB应用服务器默认的Servlet处理,如果不是静态资源的请求,才由DispatcherServlet继续处理。 -->
	<mvc:default-servlet-handler />
	<!-- 配置拦截器 -开始 -->
	<!-- <mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/**" /> 如果只写一个*,则不能拦截类似/*/*的请求。静态资源的请求需要判断不进行拦截
			配置需要拦截的请求
			<mvc:mapping path="/cart" />
			<mvc:mapping path="/wishlist" />
			<mvc:mapping path="/account" />
			<mvc:mapping path="/addwish/**" />
			<mvc:mapping path="/removewish/**" />
			<mvc:mapping path="/addcart/**" />
			<mvc:mapping path="/removecart/**" />
			<mvc:mapping path="/putcart/**" />
			<mvc:mapping path="/checkout" />
			<mvc:mapping path="/addorder" />
			<mvc:mapping path="/orderlist" />
			<mvc:mapping path="/orderdetail/**" />
			<mvc:mapping path="/checkUserPass" />
			<mvc:mapping path="/doputpass" />
			配置忽略的请求
			<mvc:exclude-mapping path="/assets/**" />
			<mvc:exclude-mapping path="/**.jsp" />
			此处配置自己书写的拦截器
			<bean class="com.situ.layoa.interceptor.ShopLoginInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors> -->
	<!-- 配置拦截器 -结束 -->
	<!-- 定义跳转的文件的前后缀 ，视图模式配置 -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- 自动给后面action的方法return的字符串加上前缀和后缀，变成一个 可用的url地址 -->
		<property name="prefix" value="/WEB-INF/pages/" />
		<property name="suffix" value=".jsp" />
	</bean>
</beans>  